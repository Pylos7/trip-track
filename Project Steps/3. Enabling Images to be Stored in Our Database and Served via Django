install pillow - pip install pillow
in project setting.py
	import os
	create 2 constants MEDIA_URL = '/media/'
	and MEDIA_ROOT = os.path.join(BASE_DIR, 'media/')
	in projects url.py we need to tell django when we are running this on our own machine localy how to serve up
	in above loc from django.conf import settings
	same dir from django.conf.urls.static import static
	if settings.DEBUG:
		urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
in app import created models
	from .models import Trip, Note
	and register them with the admin
	admin.site.register(Trip)
	admin.site.register(Note)
create and make the migrations
	py manage.py makemigrations
	py manage.py migrate
